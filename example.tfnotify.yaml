ci:
  owner:
  - type: envsubst
    value: mercari
  repo:
  - type: envsubst
    value: tfnotify
terraform:
  plan:
    template: |
      ## Plan Result
      {{if .Result}}
      <pre><code>{{ .Result }}
      </pre></code>
      {{end}}
      <details><summary>Details (Click me)</summary>

      <pre><code>{{ .CombinedOutput }}
      </pre></code></details>
